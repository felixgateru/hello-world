# Hello World

## Overview

A simple static HTML site with no frameworks or build tools required.

## Getting Started

Open `index.html` directly in your browser — no server or dependencies needed.

## Structure

- `index.html` — the main page
